pluginManagement {
    repositories {
        gradlePluginPortal()
        google()
        mavenCentral()
    }
}
dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        jcenter() // For org.wysaid:gpuimage-plus:2.6.3
    }
}
rootProject.name = "PhotoEditorCore"
include ':photoeditor'
include ':core'
project(':core').projectDir = new File('/AndroidCoreBase/core')

include ':domain'
include ':data'
